import { Box, Typography } from "@mui/material";

const MoreInfo = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        textAlign: "center",
        color: "white",
      }}
    >
      <Typography gutterBottom> TEST TEXT </Typography>
    </Box>
  );
};

export default MoreInfo;
